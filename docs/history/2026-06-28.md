# 2026-06-28 作業ログ

## LBスコア推移

| フェーズ | LB | 備考 |
|---|---|---|
| romantamrazov v2（ベースライン） | 10.4 | IDW, LGB×2 + CB |
| Phase 2-7（XGB + LGB + CB） | **10.342** | 微改善 +0.058 |
| Phase 2-8（IRLS 平滑化） | **10.312** | +0.030改善 |

---

## やったこと

### 1. Phase 2-7 結果確認

LB **10.342**（romantamrazov v2 の 10.4 から +0.058 改善）。

XGBoost 追加の効果は小幅。LGB2（weight ≈0.02）を XGBoost に置き換えたことでアンサンブル多様性が向上したが、インパクトは限定的。

### 2. 競合 discussion/notebook 調査

上位 LB 7.x notebook（boristown LB 7.295, kokinnwakashuu LB 7.159, fleongg LB 7.2, physics ensemble LB ~7.9）を pull して分析。

**主要な知見:**

| 手法 | 上位解法での位置づけ | 私の現状 |
|---|---|---|
| GR offset特徴量 (tda/tdbc/tdsc/tdpf) | 44特徴量 | romantamrazov に実装済み |
| b_well 5バリアント × 6地層 | 90特徴量 | 1バリアント（b_wls のみ） |
| IRLS degree-4 多項式平滑化 | 後処理（cv -0.37 ft） | **Phase 2-8 で追加** |
| PF likelihood ensemble (128 seeds × 4 scales) | Pipeline B の核 | 未実装 |
| Gold visible-prefix calibration | 最終後処理レイヤー | 未実装 |
| koolbox + artifact 事前計算 | 実行時間短縮 | 未使用 |

**XR Recovery（LB 7.295 の手法）:**
- 同 ID のテストウェルが訓練データに存在する場合、直接 TVT をコピー
- gate: tvt_rmse < 0.02, gr_mad < 0.50, z_mad < 0.02
- public test 3 ウェルには効くが hidden test での汎化は不明（リスクあり）

### 3. Phase 2-8: IRLS 多項式平滑化 push

`apply_irls_smooth()` を Phase 2-7 の PostProc + SG smooth の後に追加。

```python
# per well: TVT + Z を MD の degree-4 多項式でフィット（IRLS）
# anchor = last_known_TVT + last_known_Z
u = (TVT_pred + Z) - anchor
# IRLS: Cauchy 重みで外れ値を抑制（4 回反復）
w = 1 / (1 + (residual / (2 * sigma)) ** 2)
# blend: 0.25 × raw + 0.75 × poly
TVT_out = 0.25 * TVT_pred + 0.75 * poly_fit
```

**カーネル ID 一覧（更新）**

| Notebook | URL |
|---|---|
| Phase 2-7 | https://www.kaggle.com/code/gorubachohu/phase-2-7-xgboost-lgb-cb-ensemble |
| Phase 2-8 | https://www.kaggle.com/code/gorubachohu/phase-2-8-irls-polynomial-smoothing-sp45 |

---

## 翌日のタスク

1. Phase 2-8 を Run All → submit → LB 確認
2. 次の方針（Phase 2-9 候補）:
   - b_well 5バリアント追加（feature rebuild 要）
   - Gold visible-prefix calibration
   - PF likelihood ensemble
